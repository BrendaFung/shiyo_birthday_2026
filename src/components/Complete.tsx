import { motion } from 'framer-motion'; import type { Prize } from '../types'; import { Confetti } from './Confetti'; import { PaperTexture } from './PaperTexture'; import { imageMap } from '../config/imageMap';
export function Complete({ prizes, onRestart }: { prizes: Prize[]; onRestart: () => void }) { return <main className="complete-page page"><Confetti/><PaperTexture/><motion.div className="complete-hero" initial={{ scale: .8, opacity: 0 }} animate={{ scale: 1, opacity: 1 }}><span className="complete-kicker">LUCKY DAY · ALL FOUND</span><h1>🎉 恭喜全部完成 🎉</h1><img className="completion-image" src={imageMap.completionImage} alt="生日快樂插畫" onError={(event) => { event.currentTarget.style.display = 'none'; }} /><p>Happy Birthday SHIYO ❤️</p><small className="completion-blessing">希望接下來的一年，想做的事情都能順利實現，好運與驚喜也會一直陪著你。</small></motion.div><section className="prize-summary"><div className="summary-title"><span>你的 50 份驚喜</span><span>COMPLETE ♥</span></div><div className="prize-list">{prizes.map((prize, index) => <div key={prize.id}><span>{String(index + 1).padStart(2, '0')}</span><b>{prize.name}</b><i>✦</i></div>)}</div></section><button className="primary-button complete-restart" onClick={onRestart}>重新遊玩 <span>↻</span></button></main>; }
