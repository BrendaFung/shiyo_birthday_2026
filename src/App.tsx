import { useEffect, useState } from 'react'; import { Opening } from './components/Opening'; import { CardIntro } from './components/CardIntro'; import { Game } from './components/Game'; import { Complete } from './components/Complete'; import { useGameState } from './hooks/useGameState'; import { useSound } from './hooks/useSound'; import { characters, getRoundForPrize } from './config/gameConfig'; import type { Character } from './types'; import prizes from './data/prizes.json';
export default function App() { const game = useGameState(); const sound = useSound(game.state.music); const [reveal, setReveal] = useState<{ prizeId: number; prizeName: string; character: Character; blessing?: string; round?: 1 | 2 } | null>(null); const [pendingComplete, setPendingComplete] = useState(false); useEffect(() => { if (!('serviceWorker' in navigator)) return; if (import.meta.env.DEV) { navigator.serviceWorker.getRegistrations().then((registrations) => Promise.all(registrations.map((registration) => registration.unregister()))).catch(() => undefined); if ('caches' in window) caches.keys().then((keys) => Promise.all(keys.map((key) => caches.delete(key)))).catch(() => undefined); } else { navigator.serviceWorker.register('/sw.js', { updateViaCache: 'none' }).then((registration) => registration.update()).catch(() => undefined); } }, []); const open = (index: number) => { const id = game.open(index); if (id === undefined) return; sound.click(); sound.paper(); const assignment = game.assignmentForPrize(id); const character = characters.find((item) => item.id === assignment?.characterId) ?? characters[Math.floor(Math.random() * characters.length)]; setPendingComplete(game.state.opened.length + 1 === 50); setReveal({ prizeId: id, prizeName: prizes.find((prize) => prize.id === id)?.name ?? `獎品${id}`, character, blessing: assignment?.text, round: getRoundForPrize(id) }); }; const closeReveal = () => { setReveal(null); if (pendingComplete) { setPendingComplete(false); sound.win(); game.finish(); } }; const restartToHome = () => { if (window.confirm('確定要重新開始嗎？目前進度與角色祝福配置會重新洗牌。')) game.restartToHome(); }; if (game.state.phase === 'opening') return <Opening onStart={() => { sound.click(); game.setPhase('card'); }} />; if (game.state.phase === 'card') return <CardIntro onStart={() => { sound.click(); game.restart(); }} />; if (game.state.phase === 'complete') return <Complete prizes={prizes} onRestart={restartToHome} />; return <Game order={game.state.order} opened={game.state.opened} onOpen={open} count={game.state.opened.length} characters={characters} reveal={reveal} onCloseReveal={closeReveal} music={game.state.music} onMusic={game.toggleMusic} />; }
